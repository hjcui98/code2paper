> **非正式历史草稿。** 本文件保留早期命令、路径和目录结构，仅供追溯，不是
> 当前安装、运行或验收依据。请从 [README](README.md) 和
> [文档导航](docs/README.md) 开始。

export PROJECT_ROOT="/home/cuihengjia/agent/PosterGen/data/greenplm/code"
export OUT_ROOT="/home/cuihengjia/agent/code2paper_agent/output/greenplm"
export LLM_MODEL="kimi-k2.5"
python -m code2paper.main_cli run \
  --project "$PROJECT_ROOT" \
  --out-root "$OUT_ROOT" \
  --llm-provider openai \
  --llm-model "$LLM_MODEL"

author_markers.yaml + code repo
  ↓
Phase 1: CodeIntakeAgent
  → code_sources.json
  → core_snippets.json
  → method_code_alignment.json
  → code_intake_report.json
  → raw_evidence_pack.json
  ↓
Phase 2: CodeAnalyzerAgent
  → code_facts.json
  → code_method_analysis.json
  → code_alignment_ir.json
  → code_ir.json
  → entity_links.json
  → code_analysis_report.json
  ↓
Phase 3: Method Evidence Freeze
  → method_evidence.json
  → claim_evidence_map.json
  → method_evidence_review.md
  ↓
Phase 4: Deterministic Method Writer
  → method_draft.md
  → method_draft.tex
  → method_outline.json
  → terminology_table.json
  → draft_claim_map.json
  → validation reports
  ↓
Phase 5: PaperBanana Figure Generation
  → method_overview.paperbanana_input.txt
  → method_overview.png
  → method_overview.meta.json 
  


code2paper_agent/
├── src/code2paper/
│   ├── run_cli.py                     # 主入口：story-first Phase1-5
│   ├── main_cli.py                    # 子命令入口（run/intake/analyze/evidence/author/validate）
│   ├── story_first.py                 # author_markers -> story-first结构
│   ├── schemas.py
│   ├── pipeline/
│   │   ├── stage1_code_intake.py      # Phase 1
│   │   ├── stage2_code_analyze.py     # Phase 2
│   │   ├── stage3_method_evidence.py  # Phase 3
│   │   ├── stage4_author.py           # Phase 4
│   │   └── orchestrator.py
│   ├── agents/                        # 内置 CodeIntake/CodeAnalyzer 及依赖
│   │   ├── code_intake.py
│   │   ├── code_analyzer.py
│   │   ├── bridge.py                  # schema转换辅助（非旧外部bridge脚本）
│   │   ├── state/
│   │   └── utils/
│   ├── figures/                       # Phase 5
│   │   ├── backend_paperbanana.py
│   │   ├── backend_fallback.py
│   │   └── method_draft_adapter.py
│   ├── writing/
│   ├── validators/
│   ├── llm/
│   └── ...（alignment/claim_grounder等通用模块）
├── tests/
│   ├── test_run_cli.py
│   ├── test_phase4_*.py
│   ├── test_phase5_figures.py
│   ├── test_phase0_schemas.py
│   └── test_llm_runtime.py
├── scripts/
│   ├── run_with_retry.py
│   └── force_phase4_author.py
├── examples/
├── output/
├── README.md
└── pyproject.toml

RUN_TS=$(date +"%Y%m%d_%H%M%S")

PYTHONPATH=/home/cuihengjia/agent/code2paper_agent/src python3 -m code2paper.run_cli \
  /home/cuihengjia/agent/PosterGen/data/greenplm/code \
  --author /home/cuihengjia/agent/code2paper_agent/examples/author_markers.story_first.example.yaml \
  --out-root /home/cuihengjia/agent/code2paper_agent/output/greenplm_story_first_paperbanana_random_${RUN_TS} \
  --llm-provider openai \
  --llm-model kimi-k2.5 \
  --figure-backend paperbanana \
  --paperbanana-root /home/cuihengjia/agent/PosterGen/PaperBanana \
  --retrieval-setting random \
  --num-candidates 1 \
  --aspect-ratio 16:9 \
  --exp-mode demo_full \
  --allow-fidelity-fail \
  --verbose
